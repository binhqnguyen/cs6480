#!/bin/bash
#run this on CC node to register NCs' IP.
if [ "$1" == "" ]; then
	echo "Usage: ./nc_s.register \"<list of NCs' IPs>\" (don't forget the \"quote\")"
	exit
fi
sudo /usr/sbin/euca_conf --register-nodes $1
